<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:id="@+id/category_view_frame_layout">
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="250dp"
            android:id="@+id/view_category_image"
            android:scaleType="centerCrop"
            />
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/view_category_name"
            android:textSize="34sp"
            android:textColor="@android:color/white"
            android:alpha=".87"
            android:gravity="left"
            android:layout_marginLeft="24dp"
            android:layout_marginTop="190dp"
            />
    </FrameLayout>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#EEEEEE"
        android:id="@+id/task_header"
        android:layout_below="@+id/category_view_frame_layout"
        android:elevation="4dp"
        android:layout_marginBottom="4dp">
        <TextView
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:textSize="20sp"
            android:layout_marginLeft="24dp"
            android:paddingTop="12dp"
            android:paddingBottom="12dp"
            android:text="Tasks"
            android:textColor="@color/black"
            android:alpha=".87"
            />
    </RelativeLayout>

    <android.support.v7.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/task_list_recycler_view"
        android:layout_below="@+id/task_header">
    </android.support.v7.widget.RecyclerView>
</RelativeLayout>
